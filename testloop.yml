---
- name: test the loop
  hosts: localhost

  tasks:
    - name: Echo a value from the loop
      command: echo "{{ loop_item }}"
      loop:
        - 1
        - 2
        - 3
        - 4
        - 5
        - 6
      loop_control:
        loop_var: loop_item
